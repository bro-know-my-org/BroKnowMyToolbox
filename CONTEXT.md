# Bro Know My Toolbox

本词汇表定义桌面工具箱及其命令行程序共同使用的产品语言。实现和技术选择记录在 `docs/`，不属于本文件。

## Language

**工具箱（Toolbox）**：
承载、组织并运行一组内置工具的桌面产品及其命令行入口。
_Avoid_: 插件平台、启动器

**工具（Tool）**：
围绕一个明确用户目标提供完整行为的内置功能。
_Avoid_: 插件、页面、菜单项

**工具目录（Tool Catalog）**：
工具箱中可用工具及其名称、发现信息和所需能力的权威集合。
_Avoid_: 插件市场、路由表

**能力（Capability）**：
工具为完成目标而请求的高影响系统操作类别，例如文件、凭据或网络访问。
_Avoid_: 权限字符串、Tauri 插件

**授权（Consent）**：
用户允许某个工具使用一项能力的持久化产品决定；它不替代操作系统权限。
_Avoid_: 系统授权、沙箱

**便携配置（Portable Profile）**：
由显式标记启用、将普通数据保存在可整体移动目录中的运行配置。
_Avoid_: 免安装包、可执行文件旁写入

**生成计划（Generation Plan）**：
文件生成器在写入前产生的、可审查的文件创建与覆盖意图集合。
_Avoid_: 模板结果、预览文本

**语言包（Language Pack）**：
用稳定键覆盖内置展示文本的用户资源集合，缺失值回退到内置语言。
_Avoid_: 主题包、资源包

**工作区版本（Workspace Version）**：
桌面应用与 `bkmt` 共同发布和兼容的单一产品版本。
_Avoid_: 工具版本、Spark Analyzer 版本
